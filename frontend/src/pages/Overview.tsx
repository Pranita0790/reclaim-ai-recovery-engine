import { ArrowUpRight, CheckCircle2, Clock3, ShieldCheck, TrendingUp } from "lucide-react";
import { MetricCard } from "../components/ui/MetricCard";
import { PageHeader } from "../components/ui/PageHeader";
import { StatusBadge } from "../components/ui/StatusBadge";

const decisions = [{ id: "CASE-10482", action: "SMART_RETRY", value: "₹2,940", status: "Executed", time: "2 min ago" }, { id: "CASE-10479", action: "SEND_REMINDER", value: "₹1,280", status: "Approved", time: "18 min ago" }, { id: "CASE-10471", action: "DO_NOTHING", value: "₹0", status: "Policy blocked", time: "41 min ago" }, { id: "CASE-10463", action: "ESCALATE", value: "₹8,500", status: "Executed", time: "1 hr ago" }];

export function Overview() { return <><PageHeader eyebrow="OVERVIEW" title="Decision intelligence at a glance" description="Monitor how the recovery system evaluates, governs, and executes financial decisions." /><section className="metric-grid"><MetricCard label="Expected value protected" value="₹12.4L" detail="↑ 8.2% vs last 30 days" icon={TrendingUp} /><MetricCard label="Decisions evaluated" value="1,284" detail="Across 342 active cases" icon={ArrowUpRight} /><MetricCard label="Policy compliance" value="98.7%" detail="16 decisions flagged" icon={ShieldCheck} /></section><div className="overview-grid"><section className="panel quality-panel"><div className="section-heading"><div><div className="eyebrow">DECISION QUALITY</div><h2>Value by decision outcome</h2></div><span className="muted">Last 30 days</span></div><div className="quality-stat"><strong>94.2%</strong><span>of selected actions were the highest-value allowed option</span></div><div className="quality-bars"><div><span>Selected action</span><b style={{ width: "94%" }} /></div><div><span>Policy compliant</span><b style={{ width: "99%" }} /></div><div><span>Recovered value</span><b style={{ width: "76%" }} /></div></div></section><section className="panel activity-panel"><div className="section-heading"><div><div className="eyebrow">RECENT ACTIVITY</div><h2>System events</h2></div><Clock3 size={16} /></div>{["Decision batch completed", "Policy rule updated", "Recovery case evaluated"].map((item, index) => <div className="activity-row" key={item}><CheckCircle2 size={15} /><div><strong>{item}</strong><span>{index === 0 ? "12 cases · 3 min ago" : index === 1 ? "retry-window-v2 · 2 hr ago" : "CASE-10482 · 3 hr ago"}</span></div></div>)}</section></div><section className="panel table-panel"><div className="section-heading"><div><div className="eyebrow">RECENT DECISIONS</div><h2>Latest evaluated cases</h2></div><button className="text-button">View all <ArrowUpRight size={14} /></button></div><div className="table-wrap"><table><thead><tr><th>CASE</th><th>SELECTED ACTION</th><th>EXPECTED VALUE</th><th>STATUS</th><th>TIME</th></tr></thead><tbody>{decisions.map(row => <tr key={row.id}><td className="mono">{row.id}</td><td>{row.action.replaceAll("_", " ")}</td><td className="value-cell">{row.value}</td><td><StatusBadge tone={row.status === "Policy blocked" ? "warning" : "success"}>{row.status}</StatusBadge></td><td className="muted">{row.time}</td></tr>)}</tbody></table></div></section></> }
